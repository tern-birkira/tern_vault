

## ToDo

- [x] Create new `tracklabel-editor-application-context.xml` at subfolder `config` root of local asd-editor. Immitating `auronautical-editor-application-context.xml`
	- [x] Only include `editorpath` namespace declared at `polaris-editor-path-config.xsd.
	- [x] in `post_create_project_setup.sh` setup new symlink from `configs/tracklabel-editor-application-context.xml` to `/usr/share/polaris/config`. 

- [x] Update `ApplicationConfig.cpp`
	- [x] Add new constructor with application context configuration **type** parameter
		- [x] Define app (tracklabel/auronautical) types to be able to correspond between different application context files in constructor of `ApplicationConfig`.
- [x] Create new `tracklabel-configuration.properties` file in `config`.
	- [x] create new property named `tracklabel-editor.import.folder.path`, which is just the symlink folder `/usr/share/polaris/config/`


class mappings ? 