## To Fix


As of right now in @source/apps/track-label-editor/libs/asd.editor.tracklabeleditor/controllers/TrackLabelDataController.cpp  , the function
handleFieldMoved is not doing the exact same thing as removeFieldFromCurrentLayout. (missing isExtended checks()). 
Utalize our removeFieldFromCurrentLayout function

- Mabye make `moveField` in TrackLabelModel a virtual functoin, to be overwritten by extendedTrackLabelModel and VariantTrackLabelModel. 
	- Somehow emitting a different signal with the param isExtended (coming from the overwritable functions `moveField` from the derived TracklabelModels). (any other smart idea to do this?)
- *This would make the funtion `handleMoveField` in TrackLabelDataController cleaner and be able to use removeFeidlFromCurrentLayout straight up*