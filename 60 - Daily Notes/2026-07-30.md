

```internal grid
0 f1 0 f3 f4
f5 0 f6 0 0
```
|
V
```view grid
f1 f3 f4
f5 f6
```

move happen in view grid.  simple as that.
Because the repeaters store the ACTUAL rowindexes and column indexes we just have to: EITHER from qml give the row/col index OR calculate by finding field internally. (i like the qml more IF IT IS TRUE WHAT I SAID). Then the `requestMoveField` QInvokable would get row/col indexes of currently selectedField and requested Direction.
*fact check this: *


in handleMoveFieldRequest
*move selected field left*: We already know current pos from QINVOKABLE. just traverse left that position, find first isVisibile field. swap. If no find; index out of range dont move.
*move selected  field right*: same as above
*move selected field down*: First find what col index of currently selected field. then find closest match from left on the fieldlistmodel row index + 1.
- if isVisible count curr row > isVisible count beeneth row; insert selected field to end of isVisible count of beneeth, dont swap. (should work for empty line also)
- if isVisible count curr row < isVisible count beneeth row; swap matching indexes of both rows.
- if beneeth row doesn't exist; index out of bounds. don't try to do move anythiing.
*move selected field up*
same but search row above.





### TODO TOMOROWW


I am trying to implement move field with arrow keys (between visible fields, adjeceent visible fields that have absolute pos 1 and 5 in same row, must visibly swap  when 1 is selected and moved right, and internally there absolute position are also swapped, so 5 takes the place of where 1 was).

internally all fields are loaded, but the only thing that makes them invisbile is the property on the called isVisible, this means that the actual absolute indexes are set on each field (could affect beginMoveRows and endMoveRows Logic).

`beginMoveRows` fixes the issue of properties menu glitch.
now the visual ordering is fucked up, but not the internal ordering.
CHECK ALL 
- **The Destination Index Trap:** Moving a row down requires a destination index in `beginMoveRows` that is one higher than the index used in `QList::move` to account for the item's current position.
- **Repeater Structural Limitations:** The QML `Repeater` component does not natively handle dynamic row-reordering signals well and frequently fails to update its visual layout cache.
- **Missing Delegate Object Ownership:** If your QML delegate uses a custom `id` or hardcoded internal properties instead of relying strictly on the model's `modelData` or roles, it will display old cached data.
- **Incorrect Parameter Bounds:** Passing the wrong `sourceParent` or `destinationParent` indexes to `beginMoveRows` causes Qt to miscalculate the movement within a flat list.
- **Mismatched Signal Sequence:** Failing to call `endMoveRows` immediately after the C++ container modification prevents the QML view from finalizing its layout updates.
- **Missing Role Name Changes:** If the underlying data values inside the moved item changed during the move process, you must also emit `dataChanged` for that row to force a QML visual refresh.
	 - **DONT KNOW ABOUT THIS, BECAUSE OUR ROLE IS JUST A POINTER TO THE OBJECT WHICH DOESNT CHANGE**