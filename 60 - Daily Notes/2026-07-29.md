What i want:

- I don't want to calculate pixel position for example
```js
var localPos = fieldRowId.mapFromItem( rowDropAreaId, drop.x, drop.y )
```
- I rather want to rely on concrete indexes (or some other concrete way you know of that could fit better). Hence making the qml clean and easy to read.
- only allow for dragging and dropping / moving with arrow keys when a field is selected (when a FieldInterface (TrackLabelGenericFieldType counterpart) instance is selected ); visibly `highlighted: is true`
	- When dragging/moving with arrow keys i want the pop up properties edit menu to move to the left side of the tracklabel (because label grows from left->right). 

#### quirk: to be implemented (only plan refactor for now)

`Field18Type` and `EquipmentType` both exist within their own item model; `Field18ItemModel.h` and `EquipmentitemModel.cpp`

In addition to moving around the actual `TrackLabelField18Model: FieldInterface`/`TrackLabelEquipmentModel: FieldInterface`. I want to be able to move individual Items within field.
To do this:
*Drag and drop:*
single click field18/equipment-> highlights whole field, grayed out highlighted inner items -> allow dragging field in label
double click field internal item -> highlights internal item -> allow dragging internal items only within field.
*arrow keys:*
single click field18/equipment -> highlights whole field, grayed out highlighted inner items -> allow using arrow keys to move label in field
double click field on internal item -> highlightes internal item -> allow moving internal items with arrow keys only within field.



- For this to work it seems like inheriting `TrackLabelField18Model` needs to be a `QAbstractListModel` but that is kind of hard -> requires the base `FieldInterface` to be a `QAbstractListModel`. But `FieldInterface` itself has no list of items to display. 
- We will need DummyData per internal item not per `FieldInterface`.
	- see dummydata values in TrackLabelTypes.cpp

