```cpp
xmlCtxtUseOptions( parserCtxt, xmlCtxtGetOptions(parserCtxt) | XML_PARSE_NOBLANKS );
```


```json
{
    "label": "Uninstall tern-map RPM package from devcontainer",
    "type": "shell",
    "command": "sudo rpm -qa --queryformat '%{NAME}\n' | grep tern-map | xargs sudo rpm -e",
    "problemMatcher": [],
    "group": { "kind": "build", "isDefault": true }
},
{
    "label": "Uninstall polaris-backend-library RPM package from devcontainer",
    "type": "shell",
    "command": "sudo rpm -qa --queryformat '%{NAME}\n' | grep polaris-backend | xargs sudo rpm -e",
    "problemMatcher": [],
    "group": { "kind": "build", "isDefault": true }
},
{
    "label": "Uninstall tern-framework RPM package from devcontainer",
    "type": "shell",
    "command": "sudo rpm -qa --queryformat '%{NAME}\n' | grep tern-framework | xargs sudo rpm -e",
    "problemMatcher": [],
    "group": { "kind": "build", "isDefault": true }
}
```

