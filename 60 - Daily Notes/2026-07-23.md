



### ToDO: 

First: Merge from features/track-label-editor into current branch (create resolve branch from implement-evaluate-visibility, current branch). Apply those changes to current branch
## **Then Fix these changes:**

### Suggested Changes (need to read first)
Read through the whole diff. Big picture: five features landing together — a new `Active` visibility state, extended-field editing (a second layout merged onto Correlated/FlightPlanTrack), an "in holding" simulation toggle, a real category-based control-state picker, a variant switcher, and — the most substantial part — an actual implementation of the layered field-visibility algorithm (previously just a stub), backed by two long fact-check docs. The C++ visibility logic is genuinely careful. The lazy spots are mostly in the QML layer, where a couple of components were clearly copy-pasted under time pressure, plus a few small C++ inconsistencies. Roughly in priority order:



2. **The same collapsible-header widget got built three times.** `TrackLabelVariantSwitcher.qml` rebuilds something that already exists twice in `ControlStatesHeader.qml` (its own top header, and the "selected category" sub-header):

```qml
Text {
    id: headerTextId
    anchors.centerIn: parent
    text: qsTr( "Label Variant" )
    font {
        family: "Lato"
        pixelSize: 14
        bold: true
    }
    color: variantSwitcherId.isExpanded ? Style.colorScheme.mercury : Style.colorScheme.silverChalice
}
```

plus the matching collapsed-state underline:

```qml
Rectangle {
    height: 2
    anchors {
        left: parent.left
        right: parent.right
        bottom: activeVariantTextId.bottom
        bottomMargin: -4
    }
    color: Style.colorScheme.havelock
}
```

Same click-to-expand text, same color swap, same italic-underline summary when collapsed — three times over. One `Components/ExpandableSummaryHeader.qml` (`label`, `summaryText`, `expanded`, a `toggled()` signal) replaces all three; callers just supply their own option content underneath.

3. **Three copies of "find the item in this array."** `categoryById()`, `categoryIdForControlType()`, and `labelForControlType()` in `ControlStatesHeader.qml`, plus `labelForVariant()` in `TrackLabelVariantSwitcher.qml`, are all the same linear search over an array of `{label, value/id}` objects. A tiny shared `.pragma library` module — `findBy(list, field, value)` — used from both files turns four loops into one; the nested category→state lookups become a flatten + the same helper.
    
4. **The extended field row in `TrackLabelView.qml` is very likely a copy of the base one.** The new block — `Repeater` over `FieldItem`, `MouseArea` handling select/hover/right-click — mirrors the base field row that has to already exist a few lines above it (same `highlighted: editMode && fieldModel === trackLabelController.selectedField`, same hover-box calls). The only real difference is `selectExtendedField(...)` vs `selectField(...)` — exactly the split you already made cleanly on the C++ side. Worth doing the QML equivalent: one field-row delegate, parameterized by which model and which select-method to call, instead of two copies of the same `MouseArea`.
    
5. **Two small C++ inconsistencies:**
    
    - `canExtend()` is defined inline in `TrackLabelDataController.h`:
        
        ```cpp
        bool canExtend() const{    return m_activeType == commontypes::tracklabel::TrackLabelTypeVariant::Correlated        || m_activeType == commontypes::tracklabel::TrackLabelTypeVariant::FlightPlanTrack;}
        ```
        
        while everything else touched in this diff (`selectFieldInLayout`, `addFieldToLayout`, `addFieldToExtendedLayout`, `findFieldDummyData`) is declared in the header and defined in the .cpp. Move it for consistency.
    - That same "which variants can extend" , as the loop's initializer list in `syncViewLayoutMappings()`:
        
        ```cpp
        for( auto type : { commontypes::tracklabel::TrackLabelTypeVariant::Correlated,                   commontypes::tracklabel::TrackLabelTypeVariant::FlightPlanTrack } )
        ```
        
        A single `constexpr` array (or an `isExtendableVariant()` free function) used by both removes the risk of them silently diverging if a third extendable variant ever shows up.
6. **`FieldVisibilityContext`'s own doc comment isn't quite true yet.**
    
    ```cpp
    /** Bundles the two runtime flags evaluateVisibility() needs, so call sites can't
     *  transpose two same-typed bools by accident. */
    struct FieldVisibilityContext { bool isUncorrelatedLayout = false; bool isEstablishedInHolding = false; };
    ```
    
    but the only call site still builds it positionally — `{ isUncorrelatedLayout, m_runtimeData->activeVisibleInHolding() }` — exactly as transposable as two raw bools would've been. If you're on C++20, designated initializers (`.isUncorrelatedLayout = ..., .isEstablishedInHolding = ...`) would actually deliver on the comment's promise.
    

Two quick hits: the stray `FieldVisibilityModel.cpp` you deleted lived directly under `asd.editor.tracklabelfield/`, not `interface/` (which is still there and still used) — worth confirming no `CMakeLists.txt` still references it, since the `FlightTypes` cleanup earlier in the diff did include that step and this one doesn't show it. And "Active" is carrying a lot of meanings at once in this diff (a `VisibilityState` value, a control-state category label, the generic `isActive` on every `TabButton`) — nothing's wrong, just easy to misread where two or three collide in the same file.

What's already good, and worth repeating elsewhere: `selectField`/`selectFieldInLayout` and `addField`/`resolveFieldData` in `TrackLabelDataController` and `FieldsProxyModel` are exactly the right instinct — extract the shared body, keep thin public entry points. Apply that same move to items 2–4 and most of the QML duplication disappears. And `FieldInterface::evaluateVisibility`, backed by the two markdown docs, is the most careful part of the whole diff — the current form already reads cleanly against the doc's layer numbering, so that one's genuinely optional polish, not a fix.



## **Then implament these changes:**

- [ ] When altering visibility states in propertiespopup menu, give off warning (yellow highlight, with small warning triangle warning logo on field, or opacity) if new visibility rules of field will hide because of currently selected runtime states. Only visibly update the UI (calculate evaluate field visbility) when closing/updating the properties menu. 
- [ ] Create a new toggleable button (beside "Edit View" ) in same row as DisplayStatesHeader to toggle full loaded config, with visibility = true on all fields.


## **Finally:**
Split changes made from creation of branch into digestable 2-3 dependable branches (merge chain) were each merge must be operated on a  (features/track-label-editor branch

You must give me the branch names - i must create them on clickup, then also locally. only then you can create a deticated resolve merge branch for each digestable branch. A branch which is dependand on another must use the dependands resolve branch. 

resolve branch naming: tasks/DEV-<task-id>_resolve_conflict.

If this doesn't make sense or will simply not work, let me know.