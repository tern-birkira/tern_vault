
### Phase 1: Update the Domain Layer (`FieldInterface`)

* [ ] **Modify Class Definition:** Update `FieldInterface.h` to declare a flat, read-only `bool m_isVisible` property behind a `Q_PROPERTY(bool isVisible READ isVisible NOTIFY visibilityChanged)`.
* [ ] **Expose Sub-Objects:** Ensure `m_fieldText`, `m_fieldAppearance`, and `m_fieldLayout` are exposed as `CONSTANT` properties for direct QML bindings (Approach A).
* [ ] **Implement Evaluation Logic:** Write the C++ implementation for `FieldInterface::evaluateVisibility(int globalVisibilityMode, int globalControlState)`.
* [ ] Implement focus constraint logic (hide if `onlyShowOnFocus` is true and mode is `Normal`).
* [ ] Implement control state set-matching logic (hide if `controlStateVisibility` vector is populated but doesn't contain the current global control state).
* [ ] Add guard condition to only emit `visibilityChanged()` if the boolean state toggles.



### Phase 2: Refactor the Inner Row Model (`RowCellModel`)

* [ ] **Change Data Storage:** Convert `QVector<TrackLabelField> m_cells` into a raw pointer architecture: `QVector<asd::editor::tracklabelfield::FieldInterface*> m_cells`.
* [ ] **Implement Option 2 Mutation API:** - [ ] Write `insertField(int col, FieldInterface* field)` wrapping `beginInsertRows` / `endInsertRows` and calling `field->setParent(this)`.
* [ ] Write `takeField(int col)` wrapping `beginRemoveRows` / `endRemoveRows` and calling `field->setParent(nullptr)`.
* [ ] Write `removeField(int col)` to safely extract via `takeField` and run a safe `delete` operation on the heap allocation.


* [ ] **Update Lifecycle Memory Cleanup:** Add explicit pointer cleaning inside the `RowCellModel` destructor utilizing `qDeleteAll(m_cells)`.
* [ ] **Expose Role & Enums:** Update `roleNames()` to return a single `fieldObject` string mapped to `FieldObjectRole`. Add `MoveDirection` enum with `Q_ENUM` grouping.
* [ ] **Add Bubble-Up Movement Signals:** - [ ] Declare `void moveFieldRequested(...)` signal.
* [ ] Implement `Q_INVOKABLE void requestMoveField(int colIndex, MoveDirection direction)` to emit that signal.



### Phase 3: Refactor the Outer Matrix Model (`RowListModel`)

* [ ] **Update Matrix Allocation:** Update your row tracking vectors to match the pointer architecture (`QVector<RowCellModel*> m_rows`).
* [ ] **Implement Move Signal Routing Slot:** Add private slot `void handleMoveFieldRequest(RowCellModel* sourceRow, int colIndex, RowCellModel::MoveDirection direction)`.
* [ ] **Wire Connections at Birth:** Find every code pathway that spawns rows (e.g., `insertRow()`, or JSON/XML file parsing loops) and add the C++ signal-to-slot bridge:
```cpp
connect(cellModel, &RowCellModel::moveFieldRequested, this, &RowListModel::handleMoveFieldRequest);

```


* [ ] **Update Cross-Row `moveCell` Logic:** Refactor `moveCell(int fromRow, int fromCol, int toRow, int toCol)` to use `takeField()` from the source model, update the `QObject` parent to the target model if rows differ, and push via `insertField()`. Ensure `notifyRowChanged` updates both lines.

### Phase 4: Build the Master State Governor (`TrackLabelEditorModel`)

* [ ] **Define Global Context Properties:** Declare `Q_PROPERTY` tracking layers for `activeType`, `activeVisibility`, and `activeControlState`.
* [ ] **Expose Pass-Through Pointers:** Add `Q_PROPERTY` variables returning a pointer to the active `RowListModel*` (`currentLayout`) and the actively highlighted `FieldInterface*` (`selectedField`).
* [ ] **Implement Dynamic Allocator Map:** Instantiate layout states safely inside the constructor maps via `m_layouts[Type] = new RowListModel(this);`.
* [ ] **Write Selection Routing API:** Implement `Q_INVOKABLE selectField(int rowIndex, int colIndex)` to pull the targeted object out of the matrix indices via the row's `CellModelRole` and store it in `m_selectedField`.
* [ ] **Implement Cascading Evaluation Engine:** Write `reevaluateActiveLayoutVisibility()`.
* [ ] Loop sequentially through every line in the `currentLayout`.
* [ ] Extract the inner `RowCellModel*`.
* [ ] Loop over every `FieldInterface*` reference and pass the visibility mode and control variables into `field->evaluateVisibility()`.


* [ ] **Hook Up Setters:** Add calls to `reevaluateActiveLayoutVisibility()` inside the C++ property setters for `activeVisibility`, `activeControlState`, and `activeType`.

### Phase 5: Implement Infrastructure and Undo Stack

* [ ] **Write Structural Commands:** Create `MoveFieldCommand` subclassing `QUndoCommand`.
* [ ] Cache context indices (`fromRow`, `fromCol`, `toRow`, `toCol`) and the targeted `RowListModel*` inside the constructor.
* [ ] Put `model->moveCell(fromRow, fromCol, toRow, toCol)` inside `redo()`.
* [ ] Reverse indices safely inside `undo()` (`model->moveCell(toRow, toCol, fromRow, fromCol)`).



### Phase 6: Simplify the Presentation Tier (QML)

* [ ] **Clean the Canvas Field:** Open `TrackLabelField.qml` and remove any embedded JavaScript conditions checking visibility rules or layout parameters.
* [ ] **Bind Visibility Directly:** Bind your delegate's root visibility flag directly to the calculated property:
```qml
visible: fieldObject ? fieldObject.isVisible : true

```


* [ ] **Bind Attributes Natively:** Point all geometry metrics, display texts, prefixes, and labels directly down into the exposed `fieldObject` nested properties (e.g., `text: fieldObject ? fieldObject.m_fieldText.prefix : ""`).
* [ ] **Hook up Selection Inputs:** Ensure your cell mouse areas register active highlight tracking passes:
```qml
onClicked: trackLabelGovernor.selectField(rowIndex, colIndex)

```


* [ ] **Update Side Attributes Panel:** Build or update your layout properties sidebar view to point fields directly toward the context `trackLabelGovernor.selectedField` attributes.