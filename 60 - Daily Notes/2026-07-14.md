
## ToDo:

- [ ] Resolve `polaris-asd-editor` merge request
	- [ ] Resolve threads.
	- [ ] Add symlinks to `dev-bootstrap` *assign egill*
	- [ ] Add default (within cpp) `tracklabel-configuration.properties` and `class-mappings.xml` internal linkage when launching from cmake.
	- [ ] Define as types, utalize them in these structs, and default member variables.
- [ ] Resolve `tern-framework` merge request
	- [ ] Create tests for new code.
- [ ]  Start developing serializer using new `tern-framework` save xml function.