
- [ ] resolve MR editor
	- [x] resolve threads
	- [x] create default linkage of tracklabel specific application context and class mappings. when launching via cmake.

- [ ] resolve MR tern framework
	- [ ] create tests
- [ ] continue with deserializer


copilot --resume=e8ed3532-25f7-4ed6-9741-e24fbdc54e10




### ToFix 
- [x] The save button should automatically put you into `TextInput` qml component within @source/libs/asd.editor.filebrowser/resources/qml/FileBrowser.qml allowing the user to see the highlighted.


on `implement-serialize-method` branch
- [ ] The serializer.
	- [x] Each complex type of the xsd is currently saving with the namespace decleration `xmlns:tlabel="http://tern.is/polaris-asd/tracklabel"`. Remove that. It should rather be written on the root node `instances` later. The serializer currently just saves the selected `instance`, hence it shouldn't worry about this namespace decleration.
	- [x] `FieldType` is saved as internal cpp name instead of schema name, see @/usr/share/tern/polaris-asd/schemas/tracklabel.xsd and @//source/apps/track-label-editor/libs/asd.editor.tracklabeleditor/serialization/TracklabelRegistry.cpp and @/source/apps/track-label-editor/libs/asd.editor.commontypes/TrackLabelTypes.h. The same could be true for other types (simple types in xsd).
	- [x] Default values are saved into xml create a filter when instantiating xml node  to not include default values (attributes) if and only if properties of the model node is default.
		- [x] For this i highly recommneded to create defualt types within @source/apps/track-label-editor/libs/asd.editor.commontypes in whatever file seems fit.  See types
	- [x] enforce when saving  at least one tracklabel-line exists. That is only strip the line in 
```xsd
  <xs:complexType name="TrackLabelGenericType">
	    <xs:sequence>
	        <xs:element name="tracklabel-line" type="TrackLabelLineType" maxOccurs="unbounded">
	            <xs:annotation>
	                <xs:documentation> A line in the tracklabel. </xs:documentation>
	            </xs:annotation>
	        </xs:element>
	    </xs:sequence>
	</xs:complexType>
	      
```
- [x]  The follow up fix also needed is that; the program needs to recognize to insert empty lines for addField component only when there isn't already an empty line set, just like the schema enforces and allows.
 
 

**Still not pushed**
- [ ] TrackLabeLoader
	- [ ] syncAvailability syncs FieldsModel with all fields set in all tracklabels, but rather FieldsModel should only sync with the currently selected tracklabel.
		- [ ] Current implementaiton doesn't show callsign in AddField Component while editing Uncorrelated tracklabel, becuase Callsign is set in Correlated tracklabel.
- [ ] How i want to solve this: 
	- [ ] I want to have specific isAvailable roles in FieldsProxyModel.h per tracklabel variant, and extended tracklabel
	- [ ] This would mean new isAvailalerole member variables on the source model FieldsModel. It would also mean a single instance of FieldsModel in the instanciater. 

 copilot --resume=e8ed3532-25f7-4ed6-9741-e24fbdc54e10

For Types:
See 
- @/usr/share/tern/polaris-asd/schemas/polaris-shared-types.xsd
- @/usr/share/tern/polaris-asd/schemas/tracklabel-types.xsd

For Default values of types:
See
- @/usr/share/tern/polaris-asd/schemas/tracklabel.xsd 




