### Mission: correctly create evaluate visability (function on FieldInterface)

```
Það þyrfti að connecta signalið activeVisibilityChanged í controller við reevaluateActiveLayoutVisibility, og gera það svo að slot  

Þannig þá uppfærist allt sjálfkrafa
```

fact check this, if this is true, implement the approach.


The rules that decipher whether to show a field:
read @/usr/share/tern/polaris-asd/schemas/tracklabel.xsd very very carefully.
The rules are layared
- First we have the current UI visibility (normal Hover Completed) see @source/apps/track-label-editor/libs/asd.editor.commontypes/TrackLabelTypes.h
	- Second; the attribute `only-show-on-focus` 
	  If it false it is always shown any UI visibility state. On top of that we have the attribute `visible-in-holding` which states wether to show the field if in holding state.
		- Third; the element `visibility`. 
		  Its internal rules should only be checked, iff `only-show-on-focus` is `false`. The attribute `when-unselected-for-control-states` *requires application/runtime data, to evaulate* defines the set of control states for witch the field is visible in while not in focus (normal mode in our application).
			- Fourth; the elements `show-if` and `hide-if` *requires applicaoin/runtime data to evaluate* 
			  Both elements, take in key `flight-property` (a FieldType from commontypes/TrackLabelFields) and the value of the field (*runtime data*) . If field value (lookup for FieldType on currently viewing tracklabel) matches any of the set of acceptable values from `property-value`, then you may hide/show the field the underlying field (not the searched field in the tracklabel, but rather the field with the `show-if` / `hide-if` condition).


read @/workspace/polaris-asd/ and find how the the model/applicatoin calculates and applies these rules of the tracklabel. If my written rules have flaws, point them out, be explicit and site exactly where it came from the polaris-asd. If needed update accordinlgy my understanding of tracklabel visibility rules, write them down into a markdown file at root of project. Start implementing it, commit by commit, do many commits, keeping each clean so i can look back. 

commit rules: keep it short absoulute max 25 words. no special wording.




What is needed 
- global `visible-in-holding state` togglea





things to edit

- Don't use the type `ControlState` we are only using `ControlTypes` for know on, the FlightTypes files my be removed. Fix this accordingly everywhere.
- 